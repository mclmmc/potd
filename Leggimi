bin		contiene lo script principale
		va copiato in $HOME/bin

provider	script per scaricare le immagini dai vari servizi
		"National Geographic" "www.bing.com" "Local dir" ecc. ecc.
		vanno messi in $POTD_HOME/provider

src		sono solo pezzi di codice che ho salvato per uso futuro

test		copia di "provider" per fsre i test.
